### predicting true survival times T_i ###
APE_final <- mean(apply(abs(T_N - T_hat_final_N), 2, mean))
APE_first <- mean(apply(abs(T_N - T_hat_first_N), 2, mean))
APE_TF <- mean(apply(abs(T_N - T_hat_TF_N), 2, mean))
MPE_final <- mean(apply(abs(T_N - T_hat_final_N), 2, median))
MPE_first <- mean(apply(abs(T_N - T_hat_first_N), 2, median))
MPE_TF <- mean(apply(abs(T_N - T_hat_TF_N), 2, median))
AMSPE_final <- mean(apply((T_N - T_hat_final_N)^2, 2, mean))
AMSPE_first <- mean(apply((T_N - T_hat_first_N)^2, 2, mean))
AMSPE_TF <- mean(apply((T_N - T_hat_TF_N)^2, 2, mean))
APE <- c(APE_final, APE_first, APE_TF)
MPE <- c(MPE_final, MPE_first, MPE_TF)
AMSPE <- c(AMSPE_final, AMSPE_first, AMSPE_TF)
predict <- data.frame(APE = APE, MPE = MPE, AMSPE = AMSPE, row.names = c("FA-MAFT", "MAFT", "TF"))

write.csv(predict, file = "predict.csv", row.names = TRUE)